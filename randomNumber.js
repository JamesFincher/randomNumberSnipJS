randomNumber = (highNum) => Math.floor(Math.random() * highNum + 1);

//
//sample for loop that consol logs 100 random numbers between 1 and 1000
//for (i = 0; i < 100; i++) {
//   console.log(randomNumber(1000));
// }
